using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class sceneCollider : MonoBehaviour
{
    private void OnCollisionEnter(Collision other)
    {
        // if (other.gameObject.tag == "blueRing" || other.gameObject.tag == "yellowRing" || other.gameObject.tag == "redRing" || other.gameObject.tag == "greenRing")
        // {
        //     Time.timeScale = 0;
        //
        // }
    }
}
